apiVersion: v1
kind: Secret
metadata:
  name: backend-secret
  namespace: {{ .Values.global.namespace }}
type: Opaque
stringData:
  JWT_SECRET: "{{ .Values.backend.secret.JWT_SECRET }}"
  GOOGLE_GENAI_API_KEY: "{{ .Values.backend.secret.GOOGLE_GENAI_API_KEY }}"
